# ============================================================================
# Sève Web (Next.js)
# ============================================================================
# Copy to apps/web/.env.local

# Public API base URL (read in apps/web/src/lib/api-client.ts).
# Must match apps/api AUTH_URL / API_PORT and be reachable from the browser.
NEXT_PUBLIC_API_URL=http://localhost:3001
