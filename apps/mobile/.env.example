# ============================================================================
# Sève Mobile (Expo)
# ============================================================================
# Copy to apps/mobile/.env

# Expo public env vars are bundled in the JS — only put non-secret URLs here.
# Use the LAN IP (not localhost) when running on a physical device.
EXPO_PUBLIC_API_URL=http://localhost:3001
