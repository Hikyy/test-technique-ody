PG_HOST=localhost
PG_PORT=5435
PG_USER=ody
PG_PASS=ody
PG_DB=ody

# Optional: overrides PG_* if set
# DATABASE_URL=postgres://ody:ody@localhost:5435/ody

# Mailpit (Docker container, started by `make dev` / `make stack-up`)
MAIL_SMTP_PORT=1025
MAIL_WEB_PORT=8025
